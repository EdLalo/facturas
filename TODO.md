# TODO

- [ ] Inspect relevant config files to confirm PostCSS/Tailwind setup.
- [ ] Fix missing `autoprefixer` dependency (add to devDependencies).
- [x] Fix PostCSS config by removing explicit autoprefixer plugin (Next/Tailwind don't require it for this setup).
- [ ] If you still see the error, add `autoprefixer` as devDependency and reinstall.

- [ ] Re-run dev server / build to confirm error is resolved.

